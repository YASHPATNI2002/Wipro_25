const doubleNumbers = (arr) => arr.map(num => num * 2);

let inputArray = [1, 2, 3, 4];
let doubledArray = doubleNumbers(inputArray);

console.log("Input Array:", inputArray);
console.log("Doubled Array:", doubledArray);
